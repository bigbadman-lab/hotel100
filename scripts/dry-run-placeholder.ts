/**
 * Deployment / simulation / verification tooling — Gate I+.
 *
 * ABSOLUTE RULE: no production broadcast from bootstrap scripts.
 * Future broadcast paths must require explicit manual confirmation flags
 * and must follow simulate → broadcast → verify receipt.
 */
console.log("[hotel100/scripts] bootstrap placeholder — dry-run only");
console.log("Production launch, HoodLock, owner transfer, and live activation are NOT authorized.");
